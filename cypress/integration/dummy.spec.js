describe('Dummy Test', () => {
    it('Succeeds', () => {
        expect(true).to.equal(true);
    });

    it('Fails', () => {
        expect(false).to.equal(true);
    });

    it('Succeeds Again', () => {
        expect(true).to.equal(true);
    });
});